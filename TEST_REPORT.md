# NovelTool v0.13.0 测试报告

环境：Linux-6.18.35-x86_64-with-glibc2.41；Python 3.13.5。

## 本版本实际执行

```text
python -m pytest -q -W error
........................................................................ [ 25%]
........................................................................ [ 50%]
........................................................................ [ 75%]
........................................................................ [100%]
288 passed in 3.25s
```

全部本地 JavaScript 文件通过 `node --check`；Python 模块通过 `compileall`。

测试覆盖说明见 tests/ 及 README。模型响应使用模拟服务，不代表真实小模型的理解质量。没有把此前版本的浏览器点击报告沿用为本版本验收；本包尚未完成原生浏览器端到端验收。

## 界面验证

M13 使用实际 Chromium 执行发货页面的 JavaScript，经 Python HTTP 桥接连接实际服务与确定性模拟响应：生成两个独立候选，从不同候选选择片段插入草稿，手工加入空白/换行/emoji，等待 900ms 同步到程序内存，立即保存到磁盘；另一页面的版本修改导致冲突时，本地输入保持；显式重新载入后再次编辑并确认，正文逐字等于草稿。再次确认没有追加第二次，模型调用始终只有最初两次，没有 JavaScript 错误。

环境阻止 Chromium 直接访问回环地址，因此这是实际 JS + 离线 DOM + 实时 HTTP 接口验证，不是原生网络、Origin 或 CSP 完整端到端验收。没有连接真实小模型来验收写作质量、语义抽取质量或长度遵从性。

包含草稿多页面版本冲突、内存/磁盘区分、保存失败保留 dirty、任务基线过期、确认事务失败回滚、空白/范围检查与人工覆盖、重复确认幂等、历史确认后撤销、重开和 HTTP 令牌检查。更多安装/升级实测见 RELEASE_VERIFICATION.md。

Python 应用语句覆盖率：95.4%（pytest-cov，未将浏览器 JavaScript 计入）；Python 3.11 语法检查通过，运行测试使用 Python 3.13.5，不代表跨所有 Python/依赖版本验收。
