# NovelTool v0.1.0 测试报告

日期：2026-09-05。范围：M1 项目与配置存储，不包含尚未实现的小说/LLM 功能。

## 1. 实际环境

| 项目 | 本轮版本 |
|---|---|
| 系统 | Linux / x86_64 |
| Python | 3.13.5 |
| SQLite（Python 所链接） | 3.46.1 |
| FastAPI | 0.128.2 |
| Uvicorn | 0.48.0 |
| Pydantic | 2.13.4 |
| pytest | 9.0.2 |
| httpx | 0.28.1 |
| pytest-cov / coverage | 7.0.0 / 7.13.3 |
| JavaScript 语法检查 | Node.js v22.16.0 |

## 2. 自动化测试

执行：

```bash
python -m pytest -q -W error --cov=noveltool --cov-report=term-missing
```

结果：**79 passed；警告作为错误处理后仍通过。**
Python 应用代码共 544 个被统计语句，32 个未被本组测试执行，行/语句覆盖率四舍五入为 **94%**。
覆盖率不包括浏览器 JavaScript，也不表示所有情况都已经证明正确。
普通 `python -m pytest -q` 不需要覆盖率插件；测量覆盖率时额外安装 `pytest-cov`，或使用随包依赖版本参考。

主要覆盖：严格类型和范围校验、A/B 交叉限制、不可变配置、创建/打开、已有文件不覆盖、
错误数据库拒绝、schema 拒绝、进程锁、SQL CHECK/外键、整批回滚、外部版本冲突、
读路径不查询 SQLite、dirty、自动保存时机与重试、手动/退出保存、旧页面冲突、
本机 HTTP 保护、API key 字段防误存、保存失败的 503 响应。

打包检查曾发现测试辅助连接未显式关闭，已改用 `contextlib.closing`；最终运行没有遗留该警告。

## 3. 真实进程 / 真实 HTTP 验证

执行随包脚本：

```bash
python -W error tools/smoke_check.py
```

| 验收项目 | 结果 |
|---|---|
| 命令行新建项目并启动真实 Uvicorn | 通过 |
| HTTP `/health` 返回 0.1.0 / M1 | 通过 |
| 修改先只进入内存，磁盘暂未改变 | 通过 |
| 实际定时自动保存 | 通过；测试项目设为 10 秒，默认 60 秒另由时钟注入测试覆盖 |
| 第二个进程打开同一项目被拒绝 | 通过 |
| SIGINT 正常关闭时保存剩余修改 | 通过 |
| 再次启动保留已保存配置和标题 | 通过 |
| SIGKILL 后锁可重新获取、数据库可重开 | 通过 |
| SIGKILL 前未写盘的修改丢失 | 符合明确记录的延迟写盘边界，不是被隐藏的恢复能力 |

本脚本只在临时目录创建自己的测试数据，不接触用户已有项目。

## 4. 构建与安装包检查

`python -m pip wheel . --no-deps --no-build-isolation` 构建成功。
生成的 wheel 安装到另一测试虚拟环境，复用本环境已安装的第三方依赖，
在源码目录之外用隔离模式导入安装后的 NovelTool，确认不是误用源码目录中的包。

核对了打包的 SQL、HTML、JavaScript、CSS；用已安装代码创建数据库，打开项目，
请求首页、静态脚本和状态接口，均通过。
这不是一次从互联网重新安装所有依赖的干净环境测试；没有声称完成所有发行版/依赖组合兼容性验证。

## 5. 语法检查

`node --check noveltool/static/app.js` 通过。
13 个 Python 文件（应用、测试、smoke 脚本）通过 Python 3.11 语法级解析。
语法解析不等同于在 Python 3.11 上实际运行测试。

## 6. 未完成的验证与限制

本环境 Chromium 访问本地服务时返回 `ERR_BLOCKED_BY_ADMINISTRATOR`，
因此没有完成真实浏览器点击、视觉布局或跨浏览器验收，也没有把 HTTP/脚本语法测试算成它们。

未在 Python 3.11/3.12、Windows、WSL、macOS、网络文件系统上实际跑完整测试。
未做真实断电、物理磁盘故障或设备级 fsync 可靠性试验；写入失败覆盖使用可控故障注入。
没有测试模型兼容性、小说分析或生成效果，因为相关模块还没有实现。

## 7. 可复现资料

源码包包含 `tests/`、真实进程检查脚本、中文 README 和依赖版本参考。
所有发行源码包均排除了数据库、WAL、锁文件、虚拟环境、构建缓存、模型文件和实际密钥。
