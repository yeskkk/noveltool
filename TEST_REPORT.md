# NovelTool v1.0.0 实际测试记录

## 执行范围

本版基于完整 v0.20.0，仅新增“接纳全部待审”及相关 UI、事务和版本保护。数据库仍为 schema 17，没有修改模型提示、中文转换、生成或分析算法。版本从此使用 v1.x.y。

Linux / Python 3.13.5。修改前基线：390 项通过。最终 `python -m pytest -q -W error`：**411 项通过**，其中新增批量接纳测试 21 项。所有应用 JavaScript 执行 `node --check`，所有应用 Python 执行 `compileall`。确切依赖与运行信息见 `verification/environment.json`，测试输出见 `verification/tests.txt`。

没有沿用旧版覆盖率数字。测试中的模型回答为可重复样本，本版没有连接用户真实小模型；新批量接纳操作本身不请求模型。

## 新增回归覆盖

测试从默认小问题模式的普通短答建立待审观察，再验证全量接纳后实体、事实及阶段状态进入自动视图。接纳立即写盘，关程序重开后保留，正文和模型调用数量不变。

大集合测试一次接纳 **1,204 条**，把 SQLite 参数上限主动降低到 40，仍只执行一个 `BEGIN` / `COMMIT` 和一条审计记录，不依赖巨大 `IN` 参数列表或多次前端勾选请求。

已接纳、已拒绝和人工覆盖保持原样；失效来源、损坏证据、失败分析、已被最新结果替代的历史观察不被接纳。来源仍有效的早期 Revision 允许复用。部分成功分析的有效条目可以接纳，但不因此冒充完整同步。矛盾事实仍保留冲突提示；未来产生的新观察继续待审。

并发/旧页面测试覆盖正文改动、人工修改、分析新增和审核变更四种版本冲突。同一个版本并发提交两次只保存一次；刷新后没有待审项时不增加版本或审计。

错误注入覆盖更新中途失败、审计插入失败以及有记录被触发器忽略；均整批回滚，磁盘状态、设定版本和内存投影不留下部分成功。HTTP 测试覆盖同源令牌、Origin 防护、请求校验、409 冲突、审计查询和两个页面的控件。

## 实际 Chromium 页面验证

运行 `tools/bulk_review_browser.py`，使用临时数据库和独立 Python HTTP 服务；Chromium 执行实际 HTML 与 JavaScript，未发现 pageerror。验证了：

- 602 条待审、列表只展开 80 条时，按钮显示全量数；筛选为“已拒绝”仍可接纳全部待审。
- 取消确认不写库；未保存人工表单不会因批量操作刷新而丢失。
- 注入事务失败后页面显示错误并恢复按钮，数据没有部分接纳。
- 成功一次接纳 602 条，保留 1 条已拒绝和原来已接受的 1 条；正文不变，待审数归零、按钮禁用。
- 同时打开的旧时间线页面提交被版本检查拒绝，刷新后数量归零，只有一条全量审计。

**限制：**当前环境禁止 Chromium 原生访问回环地址（ERR_BLOCKED_BY_ADMINISTRATOR），所以页面 fetch 使用 Python HTTP 桥接到独立服务。本验收覆盖真实浏览器 JavaScript 和后端 HTTP 工作流，不能声称原生浏览器网络/CSP 已完整验收。记录见 `verification/bulk-review-browser.json`。该工具不属于运行时依赖。

## 安装与旧项目

完整 wheel 独立安装后，与源树逐个比较 86 个应用/资源文件的哈希，包括 17 个 SQL 文件。实际用 v0.20.0 创建并分析项目，保留自定义配置、带 emoji/空白/英文专名的正文、已接受/已拒绝观察和人工覆盖。本版直接打开后数据与版本指纹一致，不迁移、不重分析；随后一次接纳剩余 6 条，人工覆盖和正文不变，增加模型请求为 0。重开和已安装包 HTTP 验证通过，SQLite/外键检查通过。

详见 `INSTALL_VERIFICATION.md`。最终源码 ZIP 的独立解压复测结果另随发布提供 `ARCHIVE_VERIFICATION.json`。
