# v0.20.0 实际测试记录

Linux / Python 3.13.5。`python -m pytest -q`：390 项通过。应用全部 JavaScript 文件经 `node --check` 语法检查，全部 Python 经 compileall 检查。本轮未连接用户真实小模型，也没有本版真实浏览器点击验收。

## 累计回归

M18 的 337 项测试、M19 的 28 项测试均继续通过。本版新增 25 个参数化场景：汉字/外语片段启发式检测和专名保护；混合语言分段、空白和小数无损处理；短描述自动转中文；转换成功缓存与部分转换只补失败段；数字改变/姓名丢失/仍为英文/半截 JSON/长度截断/HTTP 中断均保留转换前文本；关闭转换不发额外请求；关闭内容日志后转换前文本仍可重开查看；分析原文证据不被翻译；语言设置改变使相关问答缓存失效；构思的英文风格建议转中文；候选成功或失败转换后仍可用并重新计数；取消转换保留检查点；纯思考 stop 响应是空正文而非事实；HTTP 页面包含配置和原文采用控件。

这些模型回复是可重复的测试响应，不代表真实模型的翻译或理解质量。Schema 合格和名称数字一致都不能证明语义正确。旧严格 JSON 路径仍有独立回归；新 small 路径另用不返回 Schema 的短答模拟。

## 旧项目验证

另外实际用 v0.17.0 创建 SQLite 项目并保存配置、包含 emoji/空白/英文专名的正文和手工返修。v0.20.0 打开时从 schema 14 升级至 17，生成升级前备份；旧配置保留，新协议/中文配置出现；撤销旧返修逐字恢复原文，维护完整性检查通过。备份再次由 v0.17.0 打开，原 schema 和旧正文均保留。

## 发布验证

源 ZIP 重新解压后运行测试，独立结果见 ARCHIVE_VERIFICATION.json。安装资源验证另见 INSTALL_VERIFICATION.md（实际完成后随包提供）。不沿用历史版覆盖率数字，不声称本轮浏览器原生网络或真实模型兼容性已经验收。
