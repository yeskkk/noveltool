# NovelTool v0.17.0 测试报告

## 完整测试集

`python -m pytest -q -W error`：**330 项通过**。覆盖率单独执行 `python -m coverage run --source=noveltool -m pytest -v -W error -o faulthandler_timeout=40`，330 项通过；Python 应用语句覆盖率 **95.12%**（4382 条语句，4168 条覆盖），不是分支覆盖或正确性证明。精确数据见 coverage-summary.json。

源码发布阶段在干净复制的完整源码目录再次执行相同 330 项测试，再打包 ZIP；同时执行 Python compileall 和所有本地 JavaScript 的 node --check。中间一次组合验收命令超时被终止，未计入成功结果；之后完整测试与覆盖率分别独立重跑通过。

## 关键自动验收范围

- SQLite 创建、加载、升级、事务回滚、进程锁、批量保存、失败后保留 dirty 状态。
- 不可变正文、跨块及 emoji 选区、逻辑行范围、连续撤销、确认重试幂等、旧任务/草稿冲突。
- 严格模型协议、受限 JSON 修复、Schema、逐字证据与临时引用、修复上限、失败隔离。
- 全书任务检查点、跨版本完整输入依赖、增量缺口重分析、人工审核身份保持。
- AI 返修候选、拼装与差异预览、原子替换、同步失败不回滚已确认正文。
- 后文无遗漏分块、修改/后文双来源证据、错误引用拒绝、等待同步、暂停/续跑、检查期间正文变化、输入损坏隔离。
- 完整备份携带待保存草稿并可独立打开；失败清理临时文件；历史块损坏/哈希链检测；启动恢复不自动请求模型。

## 真实 Chromium JavaScript 操作验收

`tools/browser_smoke.py` 使用一次性数据库和可重复模型响应，实际执行了浏览器中的选区转换、候选选段、拼装、自动同步草稿、手动保存、返修差异、确认、增量同步、排队一致性检查、问题标记/筛选、维护检查与历史日志、正文撤销。**未观察到 JavaScript pageerror**。操作结果见 verification/browser-report.json。

管理策略阻止本环境 Chromium 原生访问回环地址，测试采用 about:blank 安装实际 HTML/JS/CSS、fetch 经 Python FastAPI TestClient 桥接。它检验真实页面逻辑和后端交互，不代表原生浏览器网络、跨页面导航和 CSP 的完整端到端验收。页面截图在发布运行时人工检查，源码包不包含用户图像或字体。

## 安装、升级与真实进程验收

真实构建 wheel 并安装到独立目录，以独立 PYTHONPATH 和工作目录执行程序，验证本地页面、JS 和 14 个 SQL 资源齐全。v0.13.0 单独进程创建旧项目及正文/候选/草稿；新 wheel 打开时先生成旧 Schema 备份再升级，草稿保留，撤销旧正文可逐字恢复。升级前备份仍可由 v0.13.0 打开。

独立 uvicorn 通过真实本地 HTTP 运行：n=2 产生两次到临时兼容模型端点的请求；端点返回固定测试文本，不是真实语言模型。验证 SIGTERM 保存内存配置、SIGKILL 后已确认正文保留且未写盘配置按预期丢失。真实 HTTP 下载的 SQLite 备份能独立打开，不依赖源文件的 WAL。见 verification/release-smoke.json。

## 已知边界

尚未连接用户真实小模型，不能保证任意服务兼容或小说理解/创作质量。token 计数是保守估计，不是目标 tokenizer。按叙述位置计算状态不是自动求解倒叙和不可靠叙述；一致性检查只报告可能问题，分块及模型能力会漏报。无问题列表不是全文一致性证明。

只有 Python 3.13.5 / 当前 Linux 环境实际执行本轮；声明支持 Python 3.11+ 不等于所有小版本和发行版已分别验收。未保存的程序内存或浏览器输入不能靠 SQLite 恢复。磁盘硬故障、任意外部篡改、所有浏览器组合均不在完全覆盖范围。
