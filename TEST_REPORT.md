# NovelTool v0.6.0 测试报告

## 实际运行结果

- Python：3.13.5；系统：Linux；SQLite：3.46.1。
- `python -m pytest -q -W error`：**215 项通过**。
- 本次 pytest 下的 Python 行覆盖率：**95.4%**；不包含浏览器交互覆盖率。
- 针对正文包含固定种子的 250 次随机替换及逆序撤销、数据库重读核对、SQL 故障回滚。
- 包含旧 schema 升级前真实 SQLite 备份、升级失败事务回滚测试。

## 没有声称完成的验证

本轮未连接用户的真实模型服务；模拟 HTTP/输出只验证协议和程序处理，不能证明小模型的实际理解质量。
浏览器验收记录如存在，另见 BROWSER_TEST_REPORT.md；没有该文件的版本不声称做过真实浏览器点击验收。
没有跨操作系统矩阵，也未在所有受支持 Python 小版本实测；没有进行真实断电或硬盘故障实验。

## 原始 pytest 摘要

```text
........................................................................ [ 33%]
........................................................................ [ 66%]
.......................................................................  [100%]
215 passed in 2.09s
```

## 额外执行的验收

- `tools/chunk_demo.py`：自造 170005 Unicode 字符小说片段，分为 90 块，核心重组逐字一致；未调用模型。
- `tools/browser_smoke.py --bridge`：真实 Chromium DOM/JS 配合真实临时服务，正文/结构化诊断/二进制导入/分块交互通过。
  本环境回环网络受浏览器策略限制，桥接不是原生浏览器网络/CSP 端到端测试，详见独立报告。
- `tools/smoke_check.py`：实际等待 10 秒自动保存、第二进程拒绝、SIGINT 保存重开、SIGKILL 后恢复上次已提交状态通过；未写盘修改按预期丢失。
- 80 组固定种子随机文本验证核心覆盖与重叠；UTF-8/UTF-16/GB18030 编码、导入与计划写入失败回滚在 pytest 中覆盖。
