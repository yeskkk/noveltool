# 更新记录

## v0.17.0 / M17

- 新增项目维护页：启动接管计数、未完成记录、按需完整性诊断、完整项目备份下载。
- 备份前集中保存内存，SQLite backup 生成独立文件并清理发送临时文件；不接受任意文件路径。
- 查看历史修改旧/新块与有限差异、已保留的模型请求/响应及验证日志。
- 增加备份恢复/故障/损坏检测测试；实际执行 Chromium JS 桥接操作验收和独立 wheel 升级、真实 HTTP、SIGTERM/SIGKILL 测试。
- README 保留整体结构、完整流程、保存边界，并补齐恢复与备份指南。

## v0.16.0 / M16

- 返修确认可在增量同步后排队检查后文，手工返修可明确启动检查。
- 验证修改前后原文，完整分块覆盖后文；逐条检查旧/新选区与后文两类原文证据。
- 分别记录检查覆盖、失败和问题；暂停、缺失单元续跑、中断恢复及过期隔离。
- 一致性页面提供实际输入、原文引用、版本保护的人工处理标记；不自动改文或设定。
- 启动/续跑重新验证保存的完整输入清单，防止损坏数据和遗漏后文进入模型。

## v0.15.0 / M15

续写/返修共用候选流水线；新增不可变返修选区快照、行范围/鼠标选区入口、差异预览和事务性范围替换。确认后复用增量同步，支持精确撤销。

## v0.14.0 / M14

实现完整原输入依赖核验、跨版本旧观察复用、增量分块及同步页面。确认正文后可单独启动同步。加入 analysis_reuse 迁移；旧版本审核与历史 stale 判定改为来源有效性。

# 发布记录

## v0.13.0 · M13

本版本完成至 **M13：候选拼装、草稿集中保存与正文确认**。原作导入/分块 → 全书分析 → 人工设定与时间线 → 按位置状态 → 上下文装配 → n 个续写候选 → 编辑最终草稿 → 确认正文，这条主流程已经接通。没有原作时也能由想法生成可编辑的初始设定提案。确认后的自动增量语义同步尚未实现，不能把“正文已保存”理解成“设定也已同步”。

这是完整版本；整体结构与流程见 README，不需要阅读旧版 README。
