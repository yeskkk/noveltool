# 更新记录

每个版本是完整源码快照，包含代码、中文 README 和测试。

## v0.1.0 — M1：项目存储与内存底座

实现单项目 SQLite 的创建/重开、严格配置检查、内存修改、dirty 分类、可调自动保存、
手动保存和正常退出保存。新增一个离线设置页以便验收这些能力。

新增整批事务回滚测试、进程生命周期锁、旧页面乐观版本检查、磁盘版本检查、
本机 Host/Origin/会话令牌保护。实际密钥不写数据库，只保存环境变量名。

SQL schema=1 当前仅有 `project_meta` 和 `project_config` 两张表。
采用 WAL + FULL；后续实际增加正文能力时再编写迁移。
运行依赖暂为 FastAPI、Uvicorn、Pydantic，不提前安装模型相关依赖。

尚未实现正文 Block/Revision、TXT 小说导入、人物设定、模型调用和 JSON repair。
这些功能按 README 路线图逐目标实现。

## v0.0.1 — M0：最小项目骨架

实现 Python 包、命令行入口、单进程本地服务、首页和健康检查。
没有数据库，没有 LLM，没有小说逻辑。

## 下一目标：M2

先完成不可变正文 Block、稳定选区、追加/替换、精确撤销和 SQLite 迁移。
本次没有开始 M2，也没有放入未完成的正文模块。
